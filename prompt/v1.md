I want to create a prototype for a learning mobile app like duolingo that teach problem solving. The app teach the user solving problems by illustrating the problem as structured learning unit The user rarely write code, instead he answers small questions related to the problem How the app will be structured? Each topic from neetcode represents a section (like duolingo sections) in the app. Each problem from neetcode represents a unit (like duolingo units) in the app. each unit (problem) contains multiple lessons that help the user to understand the entire problem and be able to resolve it and distinguish between tradoffs

the questions must be interactive meaning:
1. the options should be visuable...... if I click on a certian option (not select it yet) I can visualize what the option mean. 
2. let's condistly dicuss this and creating ideas from it how to visualize more rather than let the user read

completing a unit should prove three things:
1. I can solve this particular problem.
2. I understand the technique/pattern used by this problem and can recognize similar problems.
3. the evolving from brute force to different apptimized solutions with understanding the trade offs

the prototype should cover two things
1. the pipeline that generate the content: it takes a url to a neetcode problem and convert it to unit
2. vanilla simple web app (minimal files) that can read and shows the generated content in a game-like way (tree + questions&answers) (daulingo experinace) (mobile like no need for desktop view)


