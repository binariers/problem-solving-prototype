{
  "type": "order-steps",
  "prompt": "Order the steps of the hash-map Two Sum solution.",
  "steps": [
    { "id": "s1", "text": "Create an empty hash map" },
    { "id": "s2", "text": "For each number, compute its complement", "visual": "complement.html" },
    { "id": "s3", "text": "If the complement is in the map, return the pair" },
    { "id": "s4", "text": "Otherwise store the number and its index" }
  ],
  "answer": ["s1", "s2", "s3", "s4"],
  "explanation": "You build the map as you go, checking for each complement before inserting the current number."
}
